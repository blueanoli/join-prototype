/*function initLogin() {
    statusVisit();
}

function statusVisit() {
  let isFirstVisit = localStorage.getItem("isFirstVisit");
  if (!isFirstVisit) {
    localStorage.setItem("isFirstVisit", "true");
    playAnimation();
  }
}

function playAnimation() {
    let animatedLogoContainer = document.querySelector('.animated-logo-container');
    let animatedLogo = document.getElementById('animated-logo');
    if (animatedLogoContainer && animatedLogo) {
      animatedLogoContainer.style.display = 'block';
      animatedLogo.style.display = 'block';
      animatedLogoContainer.offsetWidth;
      animatedLogoContainer.classList.add('animate');
      animatedLogo.classList.add('animate');
    }
  } */